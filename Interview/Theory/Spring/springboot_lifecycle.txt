✅ FINAL SPRING BOOT LIFECYCLE (CORRECT + SIMPLE)
🔑 Remember this in 3 PHASES

Startup

Context & Beans

Run & Shutdown

🟢 PHASE 1: STARTUP & PREP (Bootstrapping)
1️⃣ main() – JVM starts
SpringApplication.run(App.class, args);


What happens:

JVM starts

Classpath is read

Spring checks: Web / Reactive / Non-web app?

2️⃣ SpringApplication Initialization

Spring prepares itself:

Creates SpringApplication

Loads initial listeners

Prepares application context type

👉 No beans yet. Just prep.

3️⃣ Environment Setup

Spring loads configuration:

application.properties / application.yml

Profiles

System & command-line args

🧠 Config comes BEFORE beans (important)

🟡 PHASE 2: CONTEXT & BEANS (Core Magic)
4️⃣ ApplicationContext Creation

Spring creates the IoC container:

Web → ServletWebServerApplicationContext

Reactive → Reactive context

Non-web → Simple context

Also:

BeanFactory prepared

Context initializers applied

5️⃣ Bean Definition Loading (NO objects yet)

Spring scans and registers beans:

@ComponentScan

@Configuration

Auto-configurations

JAR scanning

It finds:

@Component
@Service
@Repository
@Controller
@Bean


📌 Important:
👉 Beans are DEFINED, not created yet.

6️⃣ Auto-Configuration

Spring Boot now applies auto-config:

Based on classpath

Based on conditions

Example:

“Oh you have JPA + Postgres? I got you.”

Auto-config adds more bean definitions.

7️⃣ Bean Instantiation & Dependency Injection

NOW objects are created 🧠

Order:

Constructor

Dependency Injection

@PostConstruct

afterPropertiesSet()

BeanPostProcessors (AOP 🔥)

👉 Beans become ready to use

🔵 PHASE 3: RUNNING & SHUTDOWN
8️⃣ ApplicationContext Refresh

This is the big switch ON 🔌

Context finalized

Event listeners fired

Embedded server starts (Tomcat, etc.)

App is officially ALIVE 🚀

9️⃣ Runners Executed

Spring calls:

CommandLineRunner

ApplicationRunner

👉 Your custom startup logic runs here.

🔟 Application Running

Normal state:

Controllers active

Filters & interceptors working

DB connections ready

Actuator exposed

1️⃣1️⃣ Shutdown Trigger

Triggered by:

Ctrl+C

SIGTERM

Kubernetes pod kill

1️⃣2️⃣ Graceful Shutdown

Spring:

Stops accepting new requests

Finishes in-flight requests

Calls @PreDestroy

Calls destroy()

Stops server

Closes context

🧹 Clean exit.

🧠 SUPER-SHORT VERSION (INTERVIEW CHEAT CODE)

Memorize this 👇

main()
→ Environment prepared
→ ApplicationContext created

→ Bean definitions loaded
→ Auto-configuration applied
→ Beans instantiated & initialized

→ Context refreshed
→ Server started
→ Runners executed
→ App running
→ Graceful shutdown
