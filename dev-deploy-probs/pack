problem 1: while using filters in boot , it gets exceuted in local but to be in dev server , we have to also override other methds other than dofilter 
so we need the init and destroy methods also , then apache will run the porject 
